export interface RecordSchema {
    firstName: string,
    lastName: string,
    surName: string,
    school: string,
    classRoom: string,
    telephone: number | null,
    isLoading:boolean,
    error: string,
}